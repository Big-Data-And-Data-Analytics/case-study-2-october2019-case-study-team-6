#Libraries
if(!require(tidyverse)){
  install.packages("tidyverse", type = "source")
}

if(!require(jsonlite)){
  install.packages("jsonlite", type = "source")
}

if(!require("jpeg")){
  install.packages("jpeg", type = "source")
}

if(!require(utf8)){
  install.packages("utf8", type = "source")
}

if(!require(RedditExtractoR)){
  install.packages("RedditExtractoR", type = "source")
}

if(!require(RSelenium)){
  install.packages("RSelenium", type = "source")
}

if(!require(xml2)){
  install.packages("xml2", type = "source")
}

if(!require(rvest)){
  install.packages("rvest", type = "source")
}

if(!require(lubridate)){
  install.packages("lubridate", type = "source")
}

if(!require(tidytext)){
  install.packages("tidytext", type = "source")
}

if(!require(mongolite)){
  install.packages("mongolite", type = "source")
}

if(!require(cld3)){
  install.packages("cld3", type = "source")
}

if(!require(reticulate)){
  install.packages("reticulate", type = "source")
}

if(!require(data.table)){
  install.packages("data.table", type = "source")
}

if(!require(textdata)){
  install.packages("textdata", type = "source")
}

if(!require(tuber)){
  install.packages("tuber", type = "source")
}
